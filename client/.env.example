# Backend API URL (Default falls back to Render URL in production and /api in development)
VITE_API_URL=https://freelancer-client-platform.onrender.com

# Backend Socket.IO URL (Optional, defaults to VITE_API_URL domain)
VITE_SOCKET_URL=https://freelancer-client-platform.onrender.com
